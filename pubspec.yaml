name: evelyn
description: "Evelyn - Una aplicación Flutter personalizada."

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  my_app: ^1.0.0
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.3
  cloud_functions: ^5.1.3
  intl: ^0.19.0
  flutter_native_splash: ^2.4.6
  http: ^1.4.0
  wakelock_plus: ^1.2.5
  shared_preferences: ^2.2.2
  connectivity_plus: ^6.0.5
  lottie: ^3.1.2
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
